UPDATE `rooms`
SET `name`                  = :name,
    `description`           = :description,
    `state`                 = :state,
    `password`              = :password,
    `users_max`             = :users_max,
    `category`              = :category,
    `tags`                  = :tags,
    `trade_state`           = :trade_state,
    `allow_pets`            = :allow_pets,
    `allow_pets_eat`        = :allow_pets_eat,
    `allow_walk_through`    = :allow_walk_through,
    `hide_wall`             = :hide_wall,
    `wall_thick`            = :wall_thick,
    `floor_thick`           = :floor_thick,
    `wall_height`           = :wall_height,
    `mute_settings`         = :mute_settings,
    `kick_settings`         = :kick_settings,
    `ban_settings`          = :ban_settings,
    `chat_type`             = :chat_type,
    `chat_balloon`          = :chat_balloon,
    `chat_speed`            = :chat_speed,
    `chat_max_distance`     = :chat_max_distance,
    `chat_flood_protection` = :chat_flood_protection,
    `floor`                 = :floor,
    `wallpaper`             = :wallpaper,
    `landscape`             = :landscape,
    `group_id`              = :group_id,
    `model_name`            = :model_name
WHERE `id` = :room_id